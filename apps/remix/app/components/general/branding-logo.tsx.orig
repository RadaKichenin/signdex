export type LogoProps = {
  className?: string;
};

export const BrandingLogo = ({ className }: LogoProps) => {
  return (
    <img
      src="/static/logo.png"
      alt="Logo"
      className={className}
      style={{ objectFit: 'contain' }}
    />
  );
};
